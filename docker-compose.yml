version: "3.8"

services:
  circuscan:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: circuscan_app
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
